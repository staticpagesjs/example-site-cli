<!DOCTYPE html>
<html lang="{{ lang|default('en') }}">
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	{%- if git is not empty %}
	<meta name="gitCommitHash" value="{{ git.commit.hash }}">
	{%- endif %}
	<meta name="generatedAt" content="{{ 'now'|date('c') }}">
	<title>{{ title|trans }} - {{ site.title|trans }}</title>
	<link rel="stylesheet" href="assets/main.css">
</head>
<body>
	<header>
		<h1>{{ site.title|trans }}</h1>
	</header>
	<main>
		<nav>
			<h1>{{ 'Index'|trans }}</h1>
			<ul>
			{% for link in navigation %}
				<li><a href="{{ url(link.url) }}">{{ link.text }}</a></li>
			{% endfor %}
			</ul>
		</nav>
		<article>
			<h1>{{ title }}</h1>
			<div>{{ breadcrumbs|map(x => x|trans)|join(' &gt; ')|raw }}</div>
			{{ body|markdown }}

			<hr>
			_context dump with custom yaml filter
			<pre><code>
			{{- _context|yaml -}}
			</code></pre>

			<hr>

			_context dump with custom json filter
			<pre><code>
			{{- _context|json -}}
			</code></pre>

			<hr>

			_context dump with built-in dump function
			<pre><code>
			{{- dump(_context) -}}
			</code></pre>

		</article>
	</main>
	<footer>
		<hr>
		<p>Generated by <a href="{{ generator.url }}">{{ generator.name }}</a> at {{ 'now'|date('YYYY-mm-dd H:ii:ss') }} from {{ header.path }}.</p>
		{% if git is not empty %}<p>This is a git project, current commit hash is {{ git.commit.hash }}.</p>{% endif %}
	</footer>
</body>
</html>
